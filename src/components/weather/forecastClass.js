export default class Forecast {
    constructor (icon, temp, description, time) {
        this.icon = icon;
        this.temp = temp;
        this.description = description;
        this.time = time;
    }
}